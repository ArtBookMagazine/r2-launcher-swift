# Readium-2 Launcher application (Swift / iOS)

## Install depencency

The project dependencies are managed with Carthage. You need to run carthage update --platform ios to fetch the dependencies and build their frameworks.

